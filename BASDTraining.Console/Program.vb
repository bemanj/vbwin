
Imports BASD.Helper

Public Class Program
    Shared Sub Main(ByVal args As String())
        'Dim inString = System.Console.ReadLine()
        'Dim crypto = New Helper(New BASD.Helper.BaseCrypto(BASDCryptoProvider.Trides))
        'Dim listEncrypt = New List(Of String)
        'listEncrypt.Add(crypto.Crypto.EncryptString(inString))
        'listEncrypt.Add(crypto.Crypto.EncryptString(inString))


        'For Each encryptedString In listEncrypt.ToArray()
        '    System.Console.WriteLine("ENCRYPTED DATA: " & encryptedString)
        '    System.Console.WriteLine("DECRYPTED DATA: " & crypto.Crypto.DecryptString(encryptedString))

        'Next
        'System.Console.ReadKey()
    End Sub
End Class


